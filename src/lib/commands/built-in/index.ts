import founders from "./founders";
import join from "./join";
import leave from "./leave";
import refreshEmotes from "./refresh-emotes";

export const commands = [founders, join, leave, refreshEmotes];
